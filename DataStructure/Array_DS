#include <stdio.h>
#include <stdlib.h>

int main()
{
    int t;
    int count = 0;
    int val = 0;
    int i = 0;
    int z = -1;
    int c,a[10000],b[10000];
    scanf("%d",&count);
    int end = count - 1;
    while(scanf("%d", &val) == 1 && i++ < count && (a[i] = val) && z++ < count)
        b[z] = a[i];
    for(c = end;c > -1;c--)
    {
        printf("%d ", b[c]);
    }
    return 0;
}
